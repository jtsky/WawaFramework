<resources>

    <!-- Base application theme. -->
    <style name="base_commonAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/base_common_colorPrimary</item>
        <item name="colorPrimaryDark">@color/base_common_colorPrimaryDark</item>
        <item name="colorAccent">@color/base_common_colorAccent</item>
    </style>

    <!--倒计时seekbar-->
    <!--name为声明的"属性集合"名，可以随便取，但是最好是设置为跟我们的View一样的名称-->
    <declare-styleable name="base_wawa_seekbar">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="base_default_progress" format="integer" />
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="base_progressbar_height" format="dimension" />
        <attr name="base_progressbar_margin" format="dimension" />
        <attr name="base_count_down" format="boolean" />
        <!--倒计时普通的样式-->
        <attr name="base_progress_style_common" format="reference" />
        <!--倒计时到最后的样式-->
        <attr name="base_progress_style_last" format="reference" />
        <!--progress 普通卡标 倒计时没有-->
        <attr name="base_progress_thumb_common" format="reference" />
        <!--progress 激活卡标 倒计时没有-->
        <attr name="base_progress_thumb_activity" format="reference" />
        <!--progress 卡标偏移量 倒计时没有-->
        <!--<attr name="base_progress_thumbOffset" format="dimension" />-->
    </declare-styleable>
</resources>
